import {describe,expect,it} from 'vitest';
import {attendanceLabel,canEditFinalized,canManage,cloneEvent,isDuplicateScan,officialAward,opportunityLabel,qrMatchesEvent,recordedMinutes,roundedAward} from './domain';
describe('attendance',()=>{it('totals multiple sessions without counting open sessions',()=>expect(recordedMinutes([{checkIn:'2026-09-24T12:00:00Z',checkOut:'2026-09-24T15:00:00Z',method:'qr'},{checkIn:'2026-09-24T17:00:00Z',checkOut:'2026-09-24T20:00:00Z',method:'manual'},{checkIn:'2026-09-24T21:00:00Z',method:'qr'}])).toBe(360));});
describe('service credit',()=>{it('keeps adjustment separate and prevents negative awards',()=>{expect(officialAward(360,60)).toBe(420);expect(officialAward(30,-90)).toBe(0)});it('rounds only official award',()=>expect(roundedAward(371,30)).toBe(360));});
describe('credential safeguards',()=>{it('rejects another event credential',()=>expect(qrMatchesEvent('event-a','event-b')).toBe(false));it('blocks immediate duplicate scans',()=>{expect(isDuplicateScan(1000,59000)).toBe(true);expect(isDuplicateScan(1000,62000)).toBe(false)});});
describe('permissions and lifecycle',()=>{it('limits organization management roles',()=>{expect(canManage('owner')).toBe(true);expect(canManage('check_in_staff')).toBe(false)});it('locks finalized and archived events',()=>{expect(canEditFinalized('finalized')).toBe(false);expect(canEditFinalized('published')).toBe(true)});});
describe('event cloning',()=>{it('copies configuration but excludes transactional records',()=>{const clone=cloneEvent({id:'1',name:'Day',status:'finalized',registrations:[1],attendance:[1],serviceAwards:[1],documents:[1],capacity:50});expect(clone).toEqual({name:'Day (Copy)',status:'draft',capacity:50});});});
describe('opportunity types',()=>{it('labels supported types clearly',()=>{expect(opportunityLabel('one_time')).toBe('One-time event');expect(opportunityLabel('multi_day')).toBe('Multi-day event');expect(opportunityLabel('ongoing_program')).toBe('Ongoing program')});it('uses program sign-in language for ongoing service',()=>{expect(attendanceLabel('ongoing_program')).toBe('Program sign-in');expect(attendanceLabel('one_time')).toBe('Event check-in')});});
